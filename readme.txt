Assignment #3 - Apr 26

major changes:

  1. used randomAccessFile for users records instead of streams 
     -> code: can be found in data.FixedRecSeq.scala

  2. added "solve cases" command for users
     -> command: typing in "case" followed by the number of the case. 
     -> code: can be found in game.Character.scala & game.Run.scala

minor changes:

  1. re-organized the project with 3 packages to make things neater
  
  2. brought back class "item"
